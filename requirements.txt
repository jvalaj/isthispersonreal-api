# requirements.txt  (top of file)
--extra-index-url https://download.pytorch.org/whl/cpu

numpy==1.26.4          # pin < 2.0 so PyTorch imports cleanly
torch==2.2.2+cpu
transformers==4.41.1
accelerate==0.30.1
pillow
fastapi
uvicorn[standard]
python-multipart
